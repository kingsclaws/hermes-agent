# Lex-Hermes v0.18.0 — Upstream Integration Plan

Date: 2026-07-01

## Priority 1 — Immediate Impact (今天合)

### 1. Security Fixes
- [ ] **MCP config persistence attack surface hardened** (PR #50476)
- [ ] **Cron credential exfiltration blocked** (PR #56196) — `base_url` overrides
- [ ] **aiohttp 3.14.1 CVE floor** + pin-drift guard (PR #56237)
- [ ] **Non-reusable sentinel for prefix secrets** (PR #54166)
- [ ] **Scope `/resume` and `/sessions` to caller origin** (PR #56378)

### 2. Scale-to-Zero + Drain
- [ ] Gateway dormant-quiesce when idle (Phase 0)
- [ ] External drain coordination for safe restart
- [ ] Busy/idle readout for lifecycle actions
- **Files**: `gateway/run.py`, `gateway/scale_to_zero.py` (new), `hermes_cli/gateway_windows.py`

### 3. Kanban Task Lifecycle Hooks
- [ ] `ctx.register_hook("kanban_task_claimed")` — task claimed
- [ ] `ctx.register_hook("kanban_task_completed")` — task completed
- [ ] `ctx.register_hook("kanban_task_blocked")` — task blocked with typed reason
- [ ] `ctx.register_hook("kanban_task_unblocked")` — task unblocked
- [ ] Backoffice relay: subscribe to kanban hooks → auto-create issues
- [ ] Backoffice relay: `kanban_task_blocked` → trigger autofix if recoverable
- **Files**: `plugins/kanban/dashboard/plugin_api.py`, `plugins/backoffice-issue-relay/__init__.py`

### 4. MoA First-Class Integration
- [ ] MoA presets as selectable models in `moa` provider
- [ ] Full-turn trace persistence to JSONL (`moa.save_traces`)
- [ ] Our existing MOA_PROVIDER env var → migrate to upstream MoA preset system
- **Files**: `tools/mixture_of_agents_tool.py`, `agent/provider_router.py`

## Priority 2 — This Week

### 5. Background Fan-Out Subagents
- [ ] `delegate_task` spawns multiple background subagents
- [ ] Non-blocking chat — results consolidated on completion
- [ ] Status bar shows running subagent count
- **Files**: `tools/delegate_tool.py`, `tui_gateway/`

### 6. Cron Reliability Wave
- [ ] Fail closed when provider drifts for unpinned jobs
- [ ] Run missed-grace jobs once
- [ ] Keep ticker alive on BaseException
- [ ] Layer enabled MCP servers onto per-job toolsets
- [ ] Guard cron model-tool path + auto-resume loop breaker
- **Files**: `cron/`, `agent/cron_*.py`

### 7. In-Place Compaction + Session
- [ ] In-place compaction (single session id), default True
- [ ] Backup includes `projects.db` and kanban boards
- **Files**: `agent/context_compressor.py`, `agent/conversation_compression.py`

### 8. Performance
- [ ] Lazy-load gateway platform adapters
- [ ] libyaml CSafeLoader for config + plugin manifests
- **Files**: `gateway/run.py`, `hermes_cli/plugins.py`

## Priority 3 — Later

### 9. Google Vertex AI Provider
- [ ] OAuth2 auto-minting from service-account JSON
- [ ] Gemini over Vertex endpoint
- **Files**: `agent/vertex_adapter.py` (new)

### 10. Desktop Features (Lex Desktop repo)
- [ ] Coding cockpit — projects sidebar, review pane
- [ ] Multi-terminal panel
- [ ] PR-style file diffs in chat
- **Repo**: `kingsclaws/lex-hermes-desktop`

## Version Bump

- [ ] `hermes_constants.py`: `HERMES_VERSION = "0.18.0"`
- [ ] `setup.py`: version bump
- [ ] `package.json` (desktop-app): version bump
- [ ] `Dockerfile.patch`: reference v0.18.0 base
- [ ] `RELEASE_v0.18.0.md`: write release notes

## Implementation Strategy

1. **Cherry-pick** security fixes from upstream (low risk, high value)
2. **Merge** scale-to-zero + drain (standalone module, clean integration)
3. **Add** kanban hooks to our existing kanban plugin
4. **Wire** backoffice relay to kanban hooks
5. **Migrate** MoA provider config to upstream preset system
6. **Sync** cron reliability fixes
7. **Port** background subagents
8. **Bump** version to 0.18.0
9. **Build** Docker image
10. **Test** on staging container

## Files to Check Upstream

Key commit range: `v0.17.0...v0.18.0`
Core files changed:
- `gateway/run.py` — scale-to-zero + drain
- `plugins/kanban/` — lifecycle hooks
- `tools/mixture_of_agents_tool.py` — MoA presets
- `tools/delegate_tool.py` — background subagents
- `cron/` — reliability
- `agent/context_compressor.py` — in-place compaction
- `agent/auxiliary_client.py` — provider security
